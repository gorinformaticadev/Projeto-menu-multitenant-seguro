describe.skip('Legacy http security headers dynamic settings placeholder', () => {
  it('is intentionally empty until the header settings suite is reintroduced', () => {
    expect(true).toBe(true);
  });
});
