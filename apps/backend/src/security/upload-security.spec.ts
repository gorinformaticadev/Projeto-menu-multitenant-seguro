describe.skip('Legacy ordem_servico upload security boundaries', () => {
  it('is pending while the ordem_servico module is absent from the current backend tree', () => {
    expect(true).toBe(true);
  });
});
