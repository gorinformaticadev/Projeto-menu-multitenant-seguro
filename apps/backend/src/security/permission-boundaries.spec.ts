describe.skip('Legacy ordem_servico permission boundaries', () => {
  it('is pending while the ordem_servico permission module is absent from the current backend tree', () => {
    expect(true).toBe(true);
  });
});
