{
  "private": true,
  "version": "2.0.0",
  "workspaces": {
    "packages": [
      "apps/*"
    ],
    "nohoist": [
      "**/@eslint/**",
      "**/eslint"
    ]
  },
  "scripts": {
    "release": "standard-version",
    "versao": "npm run release && git push --follow-tags"
  },
  "overrides": {
    "diff": "^8.0.3"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
