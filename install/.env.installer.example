# =============================================================================
# Template de variáveis para o instalador (install/install.sh)
# =============================================================================
# O instalador preenche estes valores na instalação. Em instalação não interativa
# defina as variáveis de ambiente ou use as opções -d, -e, -u, etc.
# =============================================================================

# -----------------------------
# Variáveis de instalação (novas - preenchidas no momento da instalação)
# -----------------------------
# Domínio principal de acesso à aplicação
INSTALL_DOMAIN=__DOMAIN__
# Email para certificados Let's Encrypt e notificações
LETSENCRYPT_EMAIL=__EMAIL__
# Usuário Docker Hub para pull das imagens (ex: gorinformaticadev)
DOCKERHUB_USERNAME=__DOCKERHUB_USERNAME__
# Email do primeiro usuário administrador
INSTALL_ADMIN_EMAIL=__EMAIL__
# Senha inicial do administrador (alterar após primeiro login)
INSTALL_ADMIN_PASSWORD=__ADMIN_PASSWORD__

# -----------------------------
# Configurações de domínio (geradas a partir de INSTALL_DOMAIN)
# -----------------------------
DOMAIN=__DOMAIN__
FRONTEND_URL=https://__DOMAIN__
BACKEND_URL=https://api.__DOMAIN__
NEXT_PUBLIC_API_URL=https://__DOMAIN__/api
VIRTUAL_HOST=__DOMAIN__
LETSENCRYPT_HOST=__DOMAIN__

# -----------------------------
# Banco de dados (geradas se não informadas)
# -----------------------------
DB_USER=__DB_USER__
DB_PASSWORD=__DB_PASSWORD__
DB_NAME=__DB_NAME__
# URL de conexão (montada pelo instalador)
DATABASE_URL=postgresql://__DB_USER__:__DB_PASSWORD__@db:5432/__DB_NAME__?schema=public

# -----------------------------
# Segredos (gerados pelo instalador se não informados)
# -----------------------------
JWT_SECRET=__JWT_SECRET__
ENCRYPTION_KEY=__ENCRYPTION_KEY__

# -----------------------------
# Ambiente
# -----------------------------
NODE_ENV=production
PORT=4000
