🚀 Guia rápido — Versionamento automático com 1 comando

Este guia permite que o projeto:

Analise os últimos commits (feat, fix, BREAKING CHANGE, etc.)

Calcule automaticamente a próxima versão

Gere:

CHANGELOG.md

Commit de release

Tag (vX.Y.Z)

Tudo com apenas 1 comando

Sem perguntas (sem Commitizen)

✅ 1. Pré-requisitos

Node.js ≥ 16

Git configurado

Projeto com package.json

Commits seguindo Conventional Commits
Exemplos:

feat: adicionar login
fix: corrigir erro de validação
feat!: mudança incompatível

📦 2. O que instalar

Instale apenas 1 dependência de desenvolvimento:

npm install --save-dev standard-version


(Não precisa de Commitizen)

⚙️ 3. Configuração mínima do package.json
Adicione os scripts:
{
  "scripts": {
    "release": "standard-version",
    "versao": "npm run release && git push --follow-tags"
  }
}


👉 Esse é o comando único que você vai usar.

🏷️ 4. Como funciona o versionamento automático

O standard-version analisa os commits desde a última tag:

Tipo de commit	Impacto na versão
fix:	PATCH → 0.0.X
feat:	MINOR → 0.X.0
feat!: ou BREAKING CHANGE:	MAJOR → X.0.0

📌 Exemplo:

v0.1.0 → feat: nova funcionalidade → v0.2.0
v0.2.0 → fix: ajuste simples       → v0.2.1
v0.2.1 → feat!: quebra de API      → v1.0.0

✍️ 5. Fluxo de uso no dia a dia
1️⃣ Desenvolveu algo
git add .
git commit -m "feat: adicionar busca por cliente"


(Commit normal, sem perguntas)

2️⃣ Gere versão, changelog e tag (1 comando)
npm run versao


🎯 Isso irá automaticamente:

Ler a última versão (ex: v0.2.1)

Analisar commits (feat, fix, etc.)

Gerar nova versão

Criar:

CHANGELOG.md

commit de release

tag Git (v0.3.0)

Enviar tudo para o repositório

📄 6. Estrutura gerada automaticamente

Após rodar:

npm run versao


Você terá:

CHANGELOG.md
package.json        (versão atualizada)
package-lock.json
Git Tag: vX.Y.Z

🧠 7. Dicas importantes (evita erros)
✔ Sempre use Conventional Commits
feat:
fix:
docs:
refactor:
chore:

❌ NÃO rode standard-version sem commits novos

Ele depende de commits desde a última tag.

✔ Para forçar versão manual (opcional):
npx standard-version --release-as minor

🏁 Resumo final (ultra rápido)
Instalar
npm install --save-dev standard-version

Criar versão automática
npm run versao


Sem perguntas ✔
Sem Commitizen ✔
Tag automática ✔
Changelog ✔
Baseado nos commits ✔